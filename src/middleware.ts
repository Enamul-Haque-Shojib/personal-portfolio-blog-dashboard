export { default } from "next-auth/middleware"

export const config = { matcher: ["/","/add_skill","/all_skills","/add_blog","/all_blogs","/add_project","/all_projects","/all_contactinfo","/select_features"] }